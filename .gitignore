# Godot 4+ generated cache (do not commit)
.godot/
.nomedia

# Imported translations generated from CSV
*.translation

# Export credentials and local export output
export.cfg
export_credentials.cfg
/android/
/export/
/build/

# Godot .NET / Mono (if you switch later)
.mono/
data_*/
mono_crash.*.json

# OS
.DS_Store
Thumbs.db
Desktop.ini
*~

# Editor / tooling
*.swp
.vscode/
.idea/
agent-tools/
*.lnk

# Too large for GitHub (100MB limit). Download Godot from godotengine.org.
# Asset kits already extracted under assets/.
_incoming/godot/
_incoming/godot472.zip
assets/_incoming/universal_base_characters_standard.zip
assets/_incoming/modular_outfits_fantasy_standard.zip

# Transient staging artifacts. Keep real conversion sources such as
# _incoming/audio/ visible; only generated/download byproducts are ignored.
_incoming/**/*.import
_incoming/**/*.zip
_incoming/**/*.7z
_incoming/**/*.rar
_incoming/**/*.part
_incoming/**/*.tmp
_incoming/kevdev/**
!_incoming/kevdev/.gdignore
assets/_incoming/**/*.zip
assets/_incoming/**/*.7z
assets/_incoming/**/*.rar

