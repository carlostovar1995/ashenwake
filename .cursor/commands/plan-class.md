---
description: Plan an Ashenwake spec talent tree from a short brief
---

Load `.cursor/rules/talent-trees.mdc` and `.cursor/skills/plan-class/SKILL.md`. Treat the following as the entire brief; do not ask for a form. Plan one two-column spec (WoW-style tiers at +3 per row, mixed row widths, max 3, auto row, combined ultimate). Do not implement trees, workshop changes, or a lobby picker unless the user asked to build it.

$ARGUMENTS
